<!DOCTYPE html>
<html>
  <head>
    <title>Is There Soap?</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
  <div class="container">

    <nav class="navbar navbar-expand-lg navbar-dark" style="background: #2C666E">
      <%= link_to 'Is There Soap?', root_path, class: 'navbar-brand' %>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <div class="navbar-nav">
          <% if user_signed_in? %>
          <%= link_to 'Spaces', spaces_path, class: 'nav-item nav-link' %>
          <%= link_to 'Areas', areas_path, class: 'nav-item nav-link' %>
          <%= link_to 'Items', items_path, class: 'nav-item nav-link' %>
            <% end %>

<!--          <ul class="hmenu">-->
            <%= render 'devise/menu/registration_items' %>
            <%= render 'devise/menu/login_items' %>
<!--          </ul>-->
        </div>
      </div>
    </nav>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>

    <% if user_signed_in? %>

    <%= yield %>
      <% else %>
      <div class="jumbotron" style="background-color: #B4D6D3">
        <h1 class="display-4">Is There Soap?</h1>
        <p class="lead">A simple way of keeping track of supplies across your home or office!</p>
      </div>
      <% end %>
  </div>
  </body>
</html>
